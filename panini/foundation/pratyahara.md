# Śiva Sūtras та Pratyāhāra

Цей документ досліджує механізм побудови множин фонем у граматиці Паніні.

## [PANINI]
Паніні починає свою граматику не з правил деривації, а з **Śiva Sūtras**
(14 афоризмів, `mAheSvara sUtra`), які перелічують усі фонеми санскриту
у строго визначеному порядку. Повний список у SLP1 (звірено проти
`sanskrit/learnsanskrit.org`, `content/vyakarana/sounds/the-shiva-sutras.txt`,
`PANINI-SIVA-SUTRA-PRATYAHARA`, 2026-08-13; квадратні дужки — `it`-маркер
кожного рядка, сам не входить до множини звуків):

```
 1. a i u [R]
 2. f x [k]
 3. e o [N]
 4. E O [c]
 5. h y v r [w]
 6. l [R]
 7. Y m N R n [m]
 8. J B [Y]
 9. G q D [S]
10. j b g q d [z]
11. K P C W T c w t [v]
12. k p [y]
13. z S s [r]
14. h [l]
```

Сутра **1.1.71** (`Adir antyena sahetA`, "початкова [літера] разом із
кінцевою утворює позначення") визначає механізм **pratyAhAra**:
початкова літера будь-якого рядка разом із `it`-маркером *будь-якого*
пізнішого рядка позначає всі звуки між ними включно (сам `it` — ні).

Наприклад, `aC` (від `a` в рядку 1 до `it`-маркера `c` в рядку 4)
означає множину всіх голосних: `{a, i, u, f, x, e, o, E, O}`. `haL`
(від `h` в рядку 5 до `it`-маркера `l` в рядку 14) означає всі
приголосні.

## [INTERPRETATION]
Лінгвістично, це надзвичайно геніальний спосіб стиснення (data compression). Замість того, щоб у правилі перелічувати "якщо після слова йде a, i, u, ṛ, ḷ, e, o, ai або au", Паніні просто пише "якщо йде ac". Це дозволяє створювати надкороткі й універсальні правила алгебраїчного типу.

## [MY-LISP HYPOTHESIS]
У нашій Symbolic AI системі (`My Lisp`), `pratyāhāra` ідеально мапиться на поняття **бітової маски (Bitmask)**, **множини (Set)** або **перерахування (Enum)**.
Ми не будемо зберігати їх як прості строки. Натомість, ми можемо представити всі можливі звуки як масив або хеш, а кожен `pratyāhāra` буде макросом, що повертає математичну множину для `O(1)` перевірки (pattern matching).
```lisp
;; My-Lisp Hypothesis
(def-pratyahara ac '(a i u f x e o E O))

;; При перевірці правила
(if (member next-char ac)
    (apply-sandhi)
    ...)
```
Більше того, для FPGA-архітектури `pratyāhāra` може компілюватися у фізичний hardware-регістр (бітову маску), де перевірка належності виконується за один такт через побітове `AND`.

## Джерела

- Aṣṭādhyāyī sūtra 1.1.71 — звірено проти
  `sanskrit/learnsanskrit.org`, `data/ashtadhyayi-rules.txt`
  (`PANINI-SUTRA-CITATION-VERIFICATION`, 2026-08-13): текст точно
  відповідає.
- Повний список Śiva-sūtra — звірено проти
  `content/vyakarana/sounds/the-shiva-sutras.txt` того самого
  репозиторію; перекодовано з їхньої власної ASCII-схеми в SLP1.
